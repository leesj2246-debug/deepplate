# Deep Plate (딥플레이트) Git 커밋 메시지 컨벤션 가이드라인
> **Version 1.0** | *Git Commit Message Convention*

이 가이드라인은 Deep Plate 프로젝트의 변경 이력을 투명하고 명확하게 추적하기 위해 합의된 커밋 메시지 작성 규칙입니다. 모든 팀원은 커밋 작성 시 아래 규칙을 준수합니다.

---

## 1. 커밋 메시지 기본 구조

커밋 메시지는 **첫 줄(타입과 제목)**과 **본문(선택사항)**으로 구성되며, 본문을 작성할 때는 제목과 한 줄을 띄워 구분합니다.

```text
타입: 제목 (50자 이내)

본문 내용 (무엇을, 왜 변경했는지 상세히 기재)
```

---

## 2. 제목 작성 3대 규칙

1. **50자 이내**: 제목은 한눈에 파악할 수 있도록 50자 이내로 간결하게 작성하여 가독성을 높입니다.
2. **마침표 금지**: 제목 문장의 끝에는 마침표(`.`)를 찍지 않습니다.
3. **명령형 어조 사용**: `"추가한다"`, `"수정한다"`와 같이 명확한 명령형 형태로 작성합니다.

---

## 3. 커밋 타입 (Commit Type)

커밋의 핵심 목적을 직관적으로 밝히기 위해 아래 접두사(Type)를 사용합니다.

| 타입 (Type) | 목적 | 예시 |
| :--- | :--- | :--- |
| **`feat`** | 새로운 기능 추가 | `feat: 일어 다국어 스위칭 기능을 추가한다` |
| **`fix`** | 버그 및 오류 수정 | `fix: 모바일 화면에서 헤더 잘림 현상을 수정한다` |
| **`docs`** | 문서 수정 (README, 가이드라인 등) | `docs: 커밋 컨벤션 가이드라인을 추가한다` |
| **`style`** | 코드 포맷팅, 세미콜론 누락, 띄어쓰기 등 (로직 변경 없음) | `style: index.html의 인덴트 및 코드 포맷을 수정한다` |
| **`refactor`** | 코드 구조 개선, 리팩토링 (기능 변화 없음) | `refactor: 다국어 선택 CSS 클래스 구조를 개선한다` |
| **`test`** | 테스트 코드 추가 및 수정 (서비스 로직 영향 없음) | `test: 다국어 스위칭 동작 테스트 코드를 추가한다` |
| **`chore`** | 빌드 설정, 패키지 매니저 파일(package.json 등) 수정 | `chore: 패키지 의존성을 업데이트한다` |

---

## 4. 실제 작성 예시

### 예시 1: 본문이 없는 간단한 커밋
```text
feat: 영문 폰트 스타일을 추가한다
```

### 예시 2: 본문이 포함된 상세한 커밋
```text
refactor: 다국어 Reflow 방지를 위해 CSS 스위칭 구조를 개선한다

기존 JavaScript 기반 치명적 레이아웃 깜빡임 문제를 해결하기 위해,
body[data-lang] 속성을 활용한 CSS 우선순위 방식으로 구조를 변경한다.
```
