# Deep Plate 작업 지침

이 파일은 저장소 전체에 적용되는 최소 규칙만 담는다. 세부 정보가 필요할 때만 `docs/INDEX.md`에서 관련 문서를 골라 읽는다.

## 기본 원칙

- 사용자와의 설명, 작업 보고, PR 제목과 본문은 한국어로 작성한다.
- 사용자가 요청한 범위만 수정하고 기존 변경 사항과 미추적 파일을 보존한다.
- 코드 변경은 기본적으로 `codex/` 브랜치에서 진행하고, 사용자가 정한 순서에 따라 PR을 먼저 만든 뒤 배포한다.
- 원격 저장소 변경, PR 생성, 배포처럼 외부 상태가 바뀌는 작업은 실제 성공을 확인한 뒤 완료로 보고한다.
- Tally 원본 응답 탭과 외부 서비스의 기존 데이터·권한은 명시적인 요청 없이 삭제하거나 이름을 바꾸지 않는다.

## 필요한 문서만 읽기

1. 먼저 `docs/INDEX.md`를 읽는다.
2. 새 채팅에서는 `docs/HANDOFF.md`를 읽어 직전 작업 상태와 다음 시작점을 확인한다.
3. 현재 작업과 직접 관련된 문서만 추가로 읽는다.
4. `DESIGN_GUIDE.md`와 `COMMIT_CONVENTION.md`는 현재 인코딩이 깨져 있으므로 참고 자료로만 취급한다.
5. 최신 상태가 코드나 외부 서비스와 다르면 실제 상태를 확인하고 `docs/PROJECT_STATE.md`를 갱신한다.

## 작업 인수인계

- 의미 있는 작업이 끝날 때마다 `docs/HANDOFF.md`를 최신 상태로 갱신한다.
- 작업 내용, 특이사항, 검증 결과, 브랜치·PR·미리보기·프로덕션 상태, 미해결 사항과 다음 시작점을 짧게 기록한다.
- 대화 전체를 누적하지 않고 새 채팅이 바로 이어서 일하는 데 필요한 현재 상태만 유지한다.
- 확인하지 않은 외부 상태는 완료로 적지 않으며, 비밀값·고객 정보·설문 원본 응답은 기록하지 않는다.
- 기존 미커밋 파일과 미추적 파일은 사용자 소유로 표시하고, 명시적인 확인 없이 삭제하거나 커밋하지 않는다.

## 개발 및 검증

- 기술 구성: React 19, Vite 8, TypeScript.
- 개발 서버: `npm run dev`
- 프로덕션 빌드: `npm run build`
- 코드 변경 후 `npm run lint`, `npm run test`, `npm run build`를 순서대로 실행한다.
- 화면 변경은 데스크톱과 모바일에서 주요 CTA, 언어 전환, 이미지 품질, 콘솔 오류를 확인한다.
- 검증 명령이 바뀌었을 가능성이 있으면 실제 `package.json`을 우선 확인한다.

## Git, PR, 배포

- 무관한 변경을 커밋에 포함하지 않는다.
- 커밋은 Conventional Commits 형식을 사용한다. 예: `fix: 설문 링크를 수정한다`.
- PR 본문에는 목적, 주요 변경, 검증 결과, 배포 영향을 한국어로 적는다.
- 배포 절차는 `docs/runbooks/deploy.md`를 따른다.

## 제품 및 브랜드

- 서비스명: Deep Plate.
- 핵심 경험: 여행자 일정·취향·예산·식이 조건을 바탕으로 사람이 직접 제공하는 1:1 로컬 다이닝 큐레이션.
- 주요 색상: 레드 `#B81D24`, 크림 `#FBF9F5`, 텍스트 `#111111`.
- 지원 언어: 한국어, 일본어, 영어.
- 시각 변경은 고급 편집 디자인, 충분한 여백, 선명한 이미지, 모바일 가독성을 유지한다.
